apiVersion: v1
kind: Service
metadata:
  name: {{ include "sacco-app.fullname" . }}
  labels:
    {{- include "sacco-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sacco-app.selectorLabels" . | nindent 4 }}
